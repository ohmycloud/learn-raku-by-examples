# max 和 by
# Scala maxBy(x => x.length)

role Mammal {
    has $.age;
    has $.name;
}

class Employee does Mammal {
    
}


for (18..28).roll(10) Z ('A' .. 'Z').pick(10.rand).join('');

my $e = Employee.new(:age(18), :name('Aleader'));
